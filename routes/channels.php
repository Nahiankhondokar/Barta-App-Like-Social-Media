<?php

use Illuminate\Support\Facades\Broadcast;
use Illuminate\Support\Facades\Log;

// Broadcast::channel('post.like.event', function ($user, $postUserId) {
//     // return (int) $user->id === (int) $postUserId;

//     return true;
// });
